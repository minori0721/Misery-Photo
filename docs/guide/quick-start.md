# 快速开始

## 环境要求

- Node.js 18+
- npm 9+

## 本地运行主项目

```bash
npm install
npm run dev
```

访问 `http://localhost:3000`。

## 本地运行文档站

在 docs-site 目录执行：

```bash
npm install
npm run docs:dev
```

默认访问地址为 `http://localhost:5173`。

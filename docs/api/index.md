# API 接口总览

当前版本接口按功能可分为：

- 登录与会话：登录、登出、会话校验。
- 媒体访问：图库列表、文件夹预览。
- 媒体操作：上传、删除、批量复制/移动/删除。
- 配置管理：存储桶配置读取与更新。

## 约定

- 所有受保护接口都需要登录会话。
- 非 2xx 状态会返回包含 message 的错误信息。
- 生产环境建议只通过 HTTPS 调用。

后续会补充每个接口的请求参数与响应示例。
